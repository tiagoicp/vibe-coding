type Language = variant { EN; ES; FR; IT; PT };
service : {
  get_count : () -> (nat64) query;
  greet : (text, opt Language) -> (text) query;
  increment : () -> (nat64);
  set_count : (nat64) -> (nat64);
}
